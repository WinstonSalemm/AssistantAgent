# 🔧 Исправление билда на Railway

## ❌ Проблема:
Railway не мог найти Dockerfile и определить как собрать проект.

## ✅ Решение:

### 1. Создан Dockerfile в корне проекта
- Правильная структура для multi-project solution
- Правильные пути к проектам в `src/`

### 2. Создан .dockerignore
- Исключает ненужные файлы из Docker образа

### 3. Добавлены автоматические миграции
- В `Program.cs` добавлен код для применения миграций при старте
- Работает только в Production окружении

### 4. Создан railway.json
- Указывает Railway использовать Dockerfile

---

## 🚀 Что делать дальше:

### 1. Закоммить и запушить изменения:

```bash
git add .
git commit -m "Fix: Add Dockerfile and Railway configuration"
git push
```

### 2. Railway автоматически пересоберет проект

### 3. Настроить переменные окружения в Railway:

В Railway Dashboard → Settings → Variables добавить:

```env
# Database (Railway автоматически создаст DATABASE_URL)
ConnectionStrings__DefaultConnection=${{Postgres.DATABASE_URL}}
ConnectionStrings__Redis=${{Redis.REDIS_URL}}

# OpenAI (ВАЖНО!)
OpenAI__ApiKey=sk-твой-реальный-ключ
OpenAI__Model=gpt-4o-mini
OpenAI__WhisperModel=whisper-1
OpenAI__TTSModel=tts-1
OpenAI__EmbeddingModel=text-embedding-ada-002

# ASP.NET
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://0.0.0.0:8080
```

### 4. Проверить что PostgreSQL создан:

В Railway проекте должно быть:
- ✅ PostgreSQL service (с pgvector)
- ✅ API service (твой Backend)

### 5. После деплоя проверить:

1. Health check: `https://твой-url.railway.app/health`
2. Swagger: `https://твой-url.railway.app`
3. Тестовый запрос: `POST /api/chat`

---

## 📝 Что было исправлено:

✅ **Dockerfile** - создан в корне проекта  
✅ **.dockerignore** - исключает лишние файлы  
✅ **railway.json** - конфигурация для Railway  
✅ **Auto-migrations** - автоматическое применение миграций  
✅ **Program.cs** - добавлен код для миграций  

---

## 🐛 Если всё ещё не работает:

1. **Проверь логи в Railway:**
   - Build Logs - ошибки сборки
   - Deploy Logs - ошибки запуска

2. **Проверь переменные окружения:**
   - Все ли переменные добавлены?
   - Правильный ли формат?

3. **Проверь PostgreSQL:**
   - Создан ли сервис?
   - Есть ли DATABASE_URL?

4. **Проверь OpenAI ключ:**
   - Правильный ли формат?
   - Есть ли баланс?

---

**После пуша Railway автоматически пересоберет проект! 🚀**
