# 🚀 Быстрое исправление подключения к БД

## ❌ Проблема:
Connection string найден, но подключение не работает (`canConnect: false`)

## ✅ ПРОСТОЕ РЕШЕНИЕ:

### Вариант 1: Используй внутренний URL (РЕКОМЕНДУЕТСЯ)

Railway Dashboard → API сервис → Variables

Измени `ConnectionStrings__DefaultConnection` на:
```
${{Postgres.DATABASE_URL}}
```

**Почему:** Внутренний URL (`postgres.railway.internal`) работает лучше между сервисами в одном проекте Railway.

### Вариант 2: Добавь SSL вручную

Если используешь публичный URL, добавь SSL параметры:

1. Railway Dashboard → API сервис → Variables
2. Найди `ConnectionStrings__DefaultConnection`
3. Добавь в конец: `?sslmode=Require`

Например:
```
postgresql://postgres:password@host:port/db?sslmode=Require
```

---

## 🔍 Проверка:

После изменения:
1. Railway автоматически перезапустит API
2. Проверь логи: `Database connection: True`
3. Проверь `/health/db`: `canConnect: true`

---

**Попробуй Вариант 1 - это должно сработать!** 🎯
