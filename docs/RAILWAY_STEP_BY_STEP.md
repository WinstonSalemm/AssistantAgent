# 🎯 Пошаговая инструкция: Railway Setup (с точными действиями)

## 📍 ШАГ 1: Создание PostgreSQL

### 1.1 Открой Railway
- Зайди на https://railway.app
- Войди в аккаунт
- Открой проект где задеплоен API

### 1.2 Создай PostgreSQL
1. Найди кнопку **"+ New"** (обычно вверху справа или в центре экрана)
2. Нажми на неё
3. В выпадающем меню выбери **"Database"**
4. В списке баз данных выбери **"PostgreSQL"**
5. Подожди 10-30 секунд пока Railway создаст БД

**Результат:** 
- Появится новый сервис "PostgreSQL" в списке
- Статус будет "Active" (зеленый)
- Railway автоматически создаст переменную `DATABASE_URL`

---

## 📍 ШАГ 2: Настройка переменных в API сервисе

### 2.1 Открой настройки API

1. В списке сервисов найди твой **API сервис** (тот что задеплоен, обычно называется как твой репозиторий)
2. **Нажми на него** (клик по названию или карточке)
3. Откроется страница сервиса
4. Найди вкладку **"Settings"** или иконку **⚙️** (шестеренка)
5. **Нажми на Settings**

### 2.2 Открой раздел Variables

В Settings найди раздел:
- **"Variables"** или
- **"Environment Variables"** или  
- **"Environment"**

Нажми на него чтобы открыть список переменных.

### 2.3 Добавь переменные (по одной)

Для каждой переменной:

1. Найди кнопку **"+ New Variable"** или **"Add Variable"**
2. Нажми на неё
3. Появится форма с двумя полями:
   - **Name** (или Key)
   - **Value** (или Value)

#### Переменная 1: Database Connection

**Name:** `ConnectionStrings__DefaultConnection`  
**Value:** `${{Postgres.DATABASE_URL}}`

**Как заполнить:**
1. В поле **Name** введи: `ConnectionStrings__DefaultConnection`
2. В поле **Value** введи: `${{Postgres.DATABASE_URL}}`
3. Нажми **"Add"** или **"Save"**

⚠️ **Важно:** 
- `${{Postgres.DATABASE_URL}}` - это специальный синтаксис Railway
- Railway автоматически подставит реальный connection string
- Двойное подчеркивание `__` обязательно!

#### Переменная 2: OpenAI API Key

**Name:** `OpenAI__ApiKey`  
**Value:** `sk-your-openai-api-key-here` (вставь свой реальный ключ из OpenAI dashboard)

**Как заполнить:**
1. Нажми **"+ New Variable"** снова
2. **Name:** `OpenAI__ApiKey`
3. **Value:** вставь свой API ключ (полностью, начинается с `sk-proj-...`)
4. Нажми **"Add"**

⚠️ **Важно:**
- Railway может показать поле как "Secret" - это нормально
- Ключ будет скрыт звездочками для безопасности
- После добавления Railway может перезапустить сервис автоматически

#### Переменная 3: Базовая модель GPT

**Name:** `OpenAI__Model`  
**Value:** `gpt-5-mini`

#### Переменная 4: Модель "думай глубже"

**Name:** `OpenAI__DeepThinkingModel`  
**Value:** `gpt-5.2`

#### Переменная 5: Whisper

**Name:** `OpenAI__WhisperModel`  
**Value:** `whisper-1`

#### Переменная 6: TTS

**Name:** `OpenAI__TTSModel`  
**Value:** `tts-1`

#### Переменная 7: Embeddings

**Name:** `OpenAI__EmbeddingModel`  
**Value:** `text-embedding-ada-002`

#### Переменная 8: ASP.NET Environment

**Name:** `ASPNETCORE_ENVIRONMENT`  
**Value:** `Production`

#### Переменная 9: ASP.NET URL

**Name:** `ASPNETCORE_URLS`  
**Value:** `http://0.0.0.0:8080`

---

## 📍 ШАГ 3: Проверка что всё работает

### 3.1 Проверь статус сервисов

В Railway Dashboard проверь:
- ✅ **PostgreSQL** - статус "Active" (зеленый кружок)
- ✅ **API** - статус "Active" (зеленый кружок)

Если статус красный или желтый - подожди минуту, Railway может перезапускать сервис.

### 3.2 Проверь логи

1. Открой твой **API сервис**
2. Перейди на вкладку **"Deploy Logs"** или **"Logs"**
3. Прокрути вниз до последних записей

**Что должно быть:**
```
✅ Database migrations applied successfully
✅ Starting AI Personal Assistant API
✅ Now listening on: http://[::]:8080
```

**Если видишь ошибки:**
- "Unable to connect to database" → проверь переменную `ConnectionStrings__DefaultConnection`
- "Invalid API key" → проверь переменную `OpenAI__ApiKey`

### 3.3 Проверь Health Check

1. Найди URL твоего API (обычно показывается в Railway Dashboard, типа `https://assistant-api-production.railway.app`)
2. Открой в браузере: `https://твой-url.railway.app/health`
3. Должен вернуться JSON:
```json
{
  "status": "Healthy",
  "timestamp": "2026-01-13T..."
}
```

### 3.4 Проверь Swagger UI

Открой в браузере: `https://твой-url.railway.app`

Должен открыться Swagger UI с документацией всех API endpoints!

### 3.5 Тестовый запрос

В Swagger UI:

1. Найди **POST /api/chat**
2. Нажми **"Try it out"**
3. В поле **Request body** вставь:
```json
{
  "message": "привет! как дела?",
  "useDeepThinking": false
}
```
4. Нажми **"Execute"**

**Ожидаемый результат:**
- Status: 200 OK
- Response body с ответом от AI
- Если OpenAI ключ правильный - получишь умный ответ!

---

## 🔍 Где найти Variables в Railway (визуально)

### Вариант 1: Через Settings

```
Railway Dashboard
  └── Твой проект
      └── API сервис (клик)
          └── Settings (вкладка или кнопка ⚙️)
              └── Variables (раздел)
                  └── "+ New Variable" (кнопка)
```

### Вариант 2: Через меню сервиса

Некоторые версии Railway:
- Клик правой кнопкой на сервис → "Settings"
- Или три точки (⋯) → "Settings"
- Или иконка шестеренки рядом с названием

---

## ⚠️ Частые ошибки и решения

### Ошибка: "Variable not found: Postgres.DATABASE_URL"

**Причина:** PostgreSQL сервис не создан или называется по-другому

**Решение:**
1. Проверь что PostgreSQL сервис создан
2. Если создан - проверь его название в Railway
3. В переменной используй правильное имя: `${{Postgres.DATABASE_URL}}`
4. Если PostgreSQL называется по-другому - замени `Postgres` на правильное имя

### Ошибка: "Invalid API key"

**Причина:** Неправильный формат ключа или ключ истек

**Решение:**
1. Проверь что ключ начинается с `sk-proj-...`
2. Скопируй ключ заново из OpenAI dashboard
3. Убедись что нет пробелов в начале/конце
4. Проверь баланс на https://platform.openai.com/usage

### Ошибка: "Unable to connect to database"

**Причина:** Неправильный connection string или PostgreSQL не запущен

**Решение:**
1. Проверь что PostgreSQL сервис активен (зеленый статус)
2. Проверь переменную `ConnectionStrings__DefaultConnection`:
   - Должна быть: `${{Postgres.DATABASE_URL}}`
   - НЕ должна быть пустой
3. Перезапусти API сервис: Settings → Restart

### Ошибка: "Database migrations failed"

**Причина:** Проблема с pgvector extension или БД не готова

**Решение:**
1. Подожди 1-2 минуты после создания PostgreSQL
2. Перезапусти API сервис
3. Проверь логи PostgreSQL сервиса

---

## ✅ Финальный чек-лист

После настройки проверь:

```
[ ] PostgreSQL сервис создан и активен (зеленый статус)
[ ] API сервис активен (зеленый статус)
[ ] Переменная ConnectionStrings__DefaultConnection добавлена
[ ] Значение: ${{Postgres.DATABASE_URL}}
[ ] Переменная OpenAI__ApiKey добавлена
[ ] Значение: начинается с sk-proj-...
[ ] Переменная OpenAI__Model = gpt-5-mini
[ ] Переменная OpenAI__DeepThinkingModel = gpt-5.2
[ ] Health check работает: /health возвращает {"status":"Healthy"}
[ ] Swagger UI открывается на корневом URL
[ ] Тестовый запрос /api/chat работает
[ ] В логах нет ошибок подключения к БД
[ ] В логах есть "Database migrations applied successfully"
```

---

## 🎉 Готово!

После добавления всех переменных:

1. **Railway автоматически перезапустит API** (может занять 30-60 секунд)
2. **API подключится к PostgreSQL** автоматически
3. **Миграции применятся** автоматически при старте
4. **AI агенты заработают** с OpenAI API

**Теперь твой AI ассистент полностью настроен и работает в облаке!** 🚀

---

## 📞 Если что-то не работает

1. Проверь логи в Railway Dashboard → API сервис → Deploy Logs
2. Проверь что все переменные добавлены правильно
3. Проверь что PostgreSQL активен
4. Попробуй перезапустить API сервис: Settings → Restart

**Подробная инструкция:** `docs/RAILWAY_SETUP_GUIDE.md`  
**Быстрая шпаргалка:** `docs/RAILWAY_VARIABLES_QUICK.md`
