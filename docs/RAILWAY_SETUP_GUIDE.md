# 🔧 Подробная инструкция: Настройка Railway (PostgreSQL + API ключи)

## 📋 Содержание:
1. [Создание PostgreSQL на Railway](#1-создание-postgresql-на-railway)
2. [Подключение API к PostgreSQL](#2-подключение-api-к-postgresql)
3. [Добавление OpenAI API ключей](#3-добавление-openai-api-ключей)
4. [Проверка что всё работает](#4-проверка-что-всё-работает)

---

## 1. Создание PostgreSQL на Railway

### Шаг 1: Открой Railway Dashboard

1. Зайди на https://railway.app
2. Войди в свой аккаунт
3. Открой проект где задеплоен твой API

### Шаг 2: Создай PostgreSQL сервис

1. В твоем проекте нажми кнопку **"+ New"** (вверху справа)
2. В выпадающем меню выбери **"Database"**
3. Выбери **"PostgreSQL"**

Railway автоматически:
- ✅ Создаст PostgreSQL базу данных
- ✅ Установит pgvector extension (для векторного поиска)
- ✅ Создаст переменную `DATABASE_URL` автоматически

### Шаг 3: Проверь что PostgreSQL создан

После создания ты увидишь:
- Новый сервис **"PostgreSQL"** в списке сервисов
- Зеленый индикатор что сервис работает
- В настройках PostgreSQL будет показан connection string

**Важно:** Railway автоматически создаст переменную `${{Postgres.DATABASE_URL}}` которую мы используем дальше!

---

## 2. Подключение API к PostgreSQL

### Шаг 1: Открой настройки API сервиса

1. В Railway Dashboard найди твой **API сервис** (тот что задеплоен)
2. Нажми на него чтобы открыть детали
3. Перейди на вкладку **"Settings"** (или нажми на шестеренку ⚙️)

### Шаг 2: Добавь переменные окружения для БД

В разделе **"Variables"** (или **"Environment Variables"**) нажми **"+ New Variable"** и добавь:

#### Переменная 1: Database Connection
```
Name: ConnectionStrings__DefaultConnection
Value: ${{Postgres.DATABASE_URL}}
```

**Как добавить:**
1. Нажми **"+ New Variable"**
2. В поле **"Name"** введи: `ConnectionStrings__DefaultConnection`
3. В поле **"Value"** введи: `${{Postgres.DATABASE_URL}}`
4. Нажми **"Add"** или **"Save"**

**Важно:** 
- `${{Postgres.DATABASE_URL}}` - это специальный синтаксис Railway для ссылки на переменную из другого сервиса
- Railway автоматически подставит реальный connection string из PostgreSQL сервиса
- Двойное подчеркивание `__` используется для вложенных настроек в ASP.NET Core

#### Переменная 2: Redis (опционально, если используешь)

Если создал Redis сервис:
```
Name: ConnectionStrings__Redis
Value: ${{Redis.REDIS_URL}}
```

---

## 3. Добавление OpenAI API ключей

### Шаг 1: Открой Variables в API сервисе

В том же разделе **"Variables"** где добавлял Database connection, добавь OpenAI переменные:

### Шаг 2: Добавь OpenAI API Key (КРИТИЧНО!)

```
Name: OpenAI__ApiKey
Value: sk-your-openai-api-key-here
```

**Как добавить:**
1. Нажми **"+ New Variable"**
2. В поле **"Name"** введи: `OpenAI__ApiKey`
3. В поле **"Value"** вставь свой API ключ (начинается с `sk-proj-...`)
4. Нажми **"Add"**

⚠️ **ВАЖНО:** 
- Railway может показать поле как "Secret" - это нормально, ключ будет скрыт
- После добавления Railway автоматически перезапустит сервис

### Шаг 3: Добавь настройки моделей GPT

Добавь следующие переменные:

#### Модель по умолчанию (gpt-5-mini):
```
Name: OpenAI__Model
Value: gpt-5-mini
```

#### Модель "думай глубже" (gpt-5.2):
```
Name: OpenAI__DeepThinkingModel
Value: gpt-5.2
```

#### Whisper (Speech-to-Text):
```
Name: OpenAI__WhisperModel
Value: whisper-1
```

#### TTS (Text-to-Speech):
```
Name: OpenAI__TTSModel
Value: tts-1
```

#### Embeddings (для векторного поиска):
```
Name: OpenAI__EmbeddingModel
Value: text-embedding-ada-002
```

### Шаг 4: Добавь ASP.NET настройки

#### Environment:
```
Name: ASPNETCORE_ENVIRONMENT
Value: Production
```

#### URL (порт для Railway):
```
Name: ASPNETCORE_URLS
Value: http://0.0.0.0:8080
```

---

## 4. Полный список переменных для копирования

Скопируй и добавь все эти переменные в Railway:

```env
# Database
ConnectionStrings__DefaultConnection=${{Postgres.DATABASE_URL}}

# Redis (если используешь)
ConnectionStrings__Redis=${{Redis.REDIS_URL}}

# OpenAI API Key (добавь свой реальный ключ!)
OpenAI__ApiKey=sk-your-openai-api-key-here

# OpenAI Models
OpenAI__Model=gpt-5-mini
OpenAI__DeepThinkingModel=gpt-5.2
OpenAI__WhisperModel=whisper-1
OpenAI__TTSModel=tts-1
OpenAI__EmbeddingModel=text-embedding-ada-002

# ASP.NET
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://0.0.0.0:8080
```

---

## 5. Проверка что всё работает

### Шаг 1: Проверь что сервисы запущены

В Railway Dashboard проверь:
- ✅ **PostgreSQL** сервис - статус "Active" (зеленый)
- ✅ **API** сервис - статус "Active" (зеленый)

### Шаг 2: Проверь логи деплоя

1. Открой твой **API сервис**
2. Перейди на вкладку **"Deploy Logs"** или **"Logs"**
3. Проверь что нет ошибок

**Что должно быть в логах:**
```
✅ Database migrations applied successfully
✅ Starting AI Personal Assistant API
✅ Now listening on: http://[::]:8080
```

Если видишь ошибки подключения к БД:
- Проверь что переменная `ConnectionStrings__DefaultConnection` добавлена правильно
- Проверь что PostgreSQL сервис запущен

### Шаг 3: Проверь Health Check

Открой в браузере:
```
https://твой-url.railway.app/health
```

Должен вернуть:
```json
{
  "status": "Healthy",
  "timestamp": "2026-01-13T..."
}
```

### Шаг 4: Проверь Swagger UI

Открой:
```
https://твой-url.railway.app
```

Должен открыться Swagger UI с документацией API!

### Шаг 5: Тестовый запрос

В Swagger UI попробуй:

**POST /api/chat:**
```json
{
  "message": "привет! как дела?",
  "useDeepThinking": false
}
```

Если OpenAI ключ настроен правильно - получишь ответ от AI!

---

## 6. Проверка подключения к PostgreSQL

### Способ 1: Через логи API

В Railway Dashboard → API сервис → Deploy Logs ищи:
- ✅ "Database migrations applied successfully" - значит БД подключена и миграции применены
- ❌ "Unable to connect to database" - ошибка подключения

### Способ 2: Через Railway CLI (опционально)

Если установлен Railway CLI:

```bash
railway connect postgres
```

Откроется psql консоль для прямого доступа к БД.

### Способ 3: Через тестовый запрос

Создай задачу через API:

**POST /api/tasks:**
```json
{
  "title": "Тестовая задача",
  "priority": 2
}
```

Затем проверь:

**GET /api/tasks/active**

Если задача появилась - значит БД работает и данные сохраняются!

---

## 7. Troubleshooting (Решение проблем)

### ❌ Проблема: "Unable to connect to database"

**Решение:**
1. Проверь что PostgreSQL сервис запущен (зеленый статус)
2. Проверь переменную `ConnectionStrings__DefaultConnection`:
   - Должна быть: `${{Postgres.DATABASE_URL}}`
   - НЕ должна быть пустой
   - НЕ должна быть старым connection string
3. Перезапусти API сервис: Settings → Restart

### ❌ Проблема: "OpenAI API error" или "Invalid API key"

**Решение:**
1. Проверь переменную `OpenAI__ApiKey`:
   - Должна начинаться с `sk-proj-...`
   - Не должно быть пробелов в начале/конце
   - Скопируй ключ заново из OpenAI dashboard
2. Проверь баланс на https://platform.openai.com/usage
3. Проверь что ключ не истек

### ❌ Проблема: "Database migrations failed"

**Решение:**
1. Проверь что PostgreSQL сервис запущен
2. Проверь что `ConnectionStrings__DefaultConnection` правильная
3. Проверь логи - может быть проблема с pgvector extension
4. Попробуй перезапустить API сервис

### ❌ Проблема: API не отвечает

**Решение:**
1. Проверь что сервис активен (зеленый статус)
2. Проверь Deploy Logs на ошибки
3. Проверь что порт 8080 открыт (Railway делает это автоматически)
4. Попробуй перезапустить сервис

---

## 8. Визуальная схема Railway Dashboard

```
Railway Dashboard
├── Твой проект
│   ├── PostgreSQL (сервис)
│   │   ├── Status: Active ✅
│   │   ├── Variables: DATABASE_URL (автоматически)
│   │   └── Settings → можно посмотреть connection string
│   │
│   └── API (твой Backend)
│       ├── Status: Active ✅
│       ├── Variables (Settings → Variables)
│       │   ├── ConnectionStrings__DefaultConnection = ${{Postgres.DATABASE_URL}}
│       │   ├── OpenAI__ApiKey = sk-proj-...
│       │   ├── OpenAI__Model = gpt-5-mini
│       │   ├── OpenAI__DeepThinkingModel = gpt-5.2
│       │   └── ... остальные переменные
│       ├── Deploy Logs → логи запуска
│       └── URL → https://твой-url.railway.app
```

---

## 9. Быстрая проверка чек-лист

После настройки проверь:

```
[ ] PostgreSQL сервис создан и активен
[ ] API сервис активен
[ ] Переменная ConnectionStrings__DefaultConnection добавлена
[ ] Переменная OpenAI__ApiKey добавлена
[ ] Переменные OpenAI__Model и OpenAI__DeepThinkingModel добавлены
[ ] Health check работает: /health
[ ] Swagger UI открывается
[ ] Тестовый запрос к /api/chat работает
[ ] Создание задачи через /api/tasks работает
[ ] В логах нет ошибок подключения к БД
```

---

## 10. После настройки

После того как всё настроено:

1. **API автоматически подключится к PostgreSQL** при старте
2. **Миграции применятся автоматически** (мы добавили auto-migrate в Program.cs)
3. **AI агенты будут работать** с OpenAI API
4. **Данные будут сохраняться** в облачной БД

**Теперь можешь:**
- ✅ Общаться с AI через API
- ✅ Создавать задачи и напоминания
- ✅ Все данные сохраняются в PostgreSQL на Railway
- ✅ Доступ к API из любого места (ноут, телефон, планшет)

---

## 📸 Где найти Variables в Railway

1. Открой Railway Dashboard
2. Выбери свой проект
3. Нажми на **API сервис** (твой Backend)
4. Перейди на вкладку **"Settings"** (или иконка ⚙️)
5. Найди раздел **"Variables"** или **"Environment Variables"**
6. Нажми **"+ New Variable"** для добавления

---

## 🎯 Итого:

**PostgreSQL:**
- Создай через "+ New" → Database → PostgreSQL
- Railway автоматически создаст `DATABASE_URL`
- Добавь переменную: `ConnectionStrings__DefaultConnection=${{Postgres.DATABASE_URL}}`

**OpenAI:**
- Добавь переменную: `OpenAI__ApiKey=твой-ключ`
- Добавь переменные моделей: `OpenAI__Model=gpt-5-mini`, `OpenAI__DeepThinkingModel=gpt-5.2`

**Проверка:**
- Health check: `/health`
- Swagger: корневой URL
- Тестовый запрос: `/api/chat`

---

**Готово! После добавления всех переменных Railway автоматически перезапустит API и всё заработает!** 🚀
