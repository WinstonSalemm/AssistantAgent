# 🔧 Установка pgvector расширения в Railway PostgreSQL

## ❌ Проблема:
```
extension "vector" is not available
```

Railway PostgreSQL не имеет предустановленного расширения `pgvector`.

---

## ✅ Решение:

### Вариант 1: Через Railway CLI (РЕКОМЕНДУЕТСЯ)

1. Установи Railway CLI:
   ```bash
   npm install -g @railway/cli
   ```

2. Залогинься:
   ```bash
   railway login
   ```

3. Подключись к PostgreSQL:
   ```bash
   railway connect postgres
   ```

4. В открывшейся psql консоли выполни:
   ```sql
   CREATE EXTENSION IF NOT EXISTS vector;
   ```

5. Проверь что установлено:
   ```sql
   \dx vector
   ```

6. Выйди: `\q`

---

### Вариант 2: Через Railway Dashboard (если есть доступ к SQL консоли)

1. Railway Dashboard → Postgres сервис → Database
2. Открой SQL консоль (если доступна)
3. Выполни: `CREATE EXTENSION IF NOT EXISTS vector;`

---

### Вариант 3: Временное решение - отключить pgvector

Если не можешь установить расширение сейчас, можно временно отключить его:

1. В `AssistantDbContext.cs` закомментируй:
   ```csharp
   // modelBuilder.HasPostgresExtension("vector");
   ```

2. В миграции закомментируй:
   ```csharp
   // migrationBuilder.AlterDatabase()
   //     .Annotation("Npgsql:PostgresExtension:vector", ",,");
   ```

3. Приложение будет работать, но без векторного поиска.

---

## 🎯 После установки:

После установки расширения:
1. Railway автоматически перезапустит API (или перезапусти вручную)
2. В логах увидишь: `✅ pgvector extension ready`
3. Миграции применятся успешно
4. Векторный поиск заработает

---

**Рекомендую Вариант 1 - через Railway CLI!** 🚀
