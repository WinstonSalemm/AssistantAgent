# 🔍 Проверка логов Railway для диагностики БД

## 📋 Что делать:

### 1. Открой логи Railway

1. Зайди на https://railway.app
2. Выбери проект → **API сервис** (perceptive-perception)
3. Перейди на вкладку **"Deploy Logs"** или **"Logs"**

### 2. Ищи эти строки:

#### ✅ Если всё ОК:
```
✅ Database migrations applied successfully
✅ All migrations are up to date
Database connection: True
```

#### ❌ Если есть проблема:

**Ошибка подключения:**
```
❌ Database connection failed: ...
❌ Cannot connect to database!
```

**Ошибка миграций:**
```
❌ Error applying database migrations: ...
```

**Ошибка connection string:**
```
ConnectionStrings:DefaultConnection is not configured
```

### 3. Скопируй ошибку

Найди строку с ошибкой и скопируй:
- Полное сообщение об ошибке
- Stack trace (если есть)
- Inner exception (если есть)

---

## 🎯 Типичные ошибки и решения:

### Ошибка 1: "Format of the initialization string..."
**Причина:** Connection string пустой или неправильного формата  
**Решение:** Проверь переменную `ConnectionStrings__DefaultConnection` в Railway Variables

### Ошибка 2: "Connection refused" или "Host not found"
**Причина:** PostgreSQL сервис недоступен или неправильный хост  
**Решение:** 
- Проверь что Postgres сервис запущен (статус "Online")
- Убедись что используется `${{Postgres.DATABASE_URL}}` а не прямой connection string

### Ошибка 3: "Authentication failed" или "Password incorrect"
**Причина:** Неправильный пароль или пользователь  
**Решение:** Используй `${{Postgres.DATABASE_URL}}` - Railway автоматически подставит правильные credentials

### Ошибка 4: "Database does not exist"
**Причина:** База данных не создана  
**Решение:** Railway создает БД автоматически, но может потребоваться перезапуск Postgres сервиса

---

## 📸 Где найти логи:

1. Railway Dashboard → API сервис → **Deploy Logs** (логи деплоя)
2. Railway Dashboard → API сервис → **Logs** (runtime логи)
3. Railway Dashboard → Postgres сервис → **Logs** (логи БД)

---

**После проверки логов - скопируй ошибку и покажи мне!** 🔍
